# 3D Calc Pro — Архитектура и Логика работы калькулятора

Документация объясняет процесс ценообразования, основные формулы расчетов и структуру калькулятора 3D печати.

---

## 1. Основная логика расчета

Итоговая стоимость формируется из следующих компонентов:
1. **Базовая себестоимость деталей** с учетом брака (материал + амортизация принтера + электроэнергия).
2. **Процентная наценка** (определяется индивидуально для каждого типа пластика).
3. **Коэффициент сложности печати** (применяется к каждой детали поверх базы и наценки).
4. **Услуги 3D-моделирования** (зависят от времени, уровня работы и количества примерок).
5. **Дополнительный ручной труд** (опциональная сумма за сборку или постобработку).
6. **Доплата до минимального заказа** (срабатывает, если итог ниже установленного порога).

---

## 2. Пошаговый расчет для каждой детали

Каждая добавленная на печать деталь просчитывается индивидуально. Основная логика находится в хуке `useCalculator.ts`.

### 2.1. Расчет себестоимости и учет брака
Задается параметр **Коэффициент брака** (`wasteFactor`), по умолчанию он равен `1.1` (что означает надбавку в +10% на непредвиденные проблемы и поддержкки). Он применяется не только к пластику, но и ко времени (амортизации и свету).

*   **Затраты на пластик:**
    `Вес (в кг) × Цена за кг пластика` 
*   **Амортизация оборудования:**
    `Время печати (в часах) × Амортизация в час (250 ₸)`
*   **Электроэнергия:**
    `Время печати (в часах) × Свет в час (40 ₸)`

**База детали** (`partBase`) = `(Пластик + Амортизация + Свет) × wasteFactor`

### 2.2. Наценка (Markup)
У каждого вида пластика своя заданная в настройках наценка (например, для PETG — 225%, для ASA — 150%). 
Наценка высчитывается от Базы:
**Наценка детали** (`partMarkup`) = `partBase × (Процент наценки / 100)`

### 2.3. Сложность печати (Complexity)
Деталь может иметь один из 4 уровней сложности. Сложность умножает уже посчитанную сумму (`partBase + partMarkup`) на определенный коэффициент (`factor`):
*   🟢 **Простая (Тех. детали):** Коэффициент **1.0** (без изменений)
*   🟡 **Средняя (Стандарт):** Коэффициент **1.1** (+10%)
*   🟠 **Сложная (Поддержки):** Коэффициент **1.3** (+30%)
*   🔴 **Ультра (Фигурки):** Коэффициент **1.6** (+60%)

**Итог по детали** = `(partBase + partMarkup) × factor`

---

## 3. Расчет услуг 3D-Моделирования

Модуль моделирования (`ModelingCalculator.tsx`) позволяет гибко оценивать работу в CAD-программах (например, Fusion 360). 

Зависит от трех параметров:
1. **Время (в часах)**
2. **Сложность работы (Ставка в час)**
   * **Простая** (Базовый реверс, кронштейны): *4 000 ₸/час*
   * **Средняя** (Сложная геометрия, «додумывание»): *6 500 ₸/час*
   * **Профессиональная** (Сложные сборки, срочность): *10 000 ₸/час*
3. **Количество итераций / примерок**
   * За каждую примерку черновика берется фиксированная сумма (по умолчанию *1 000 ₸*).

**Итого моделирование** = `(Часы × Ставка сложности) + (Кол-во итераций × Цена итерации)`

---

## 4. Сведение итогов и минимальный заказ

Когда все детали и услуги подсчитаны, суммы складываются:
**Предварительный итог** = `Сумма всех деталей + Моделирование + Ручной Труд`

Калькулятор проверяет параметр **Минимальная цена заказа** (`minOrderPrice`, по умолчанию 2000 ₸). 
Если предварительный итог составил, например, 1400 ₸, программа автоматически добавляет "Надбавку за минимальный заказ" (`minOrderSurcharge`) в размере 600 ₸.

**Окончательная цена заказа:**
Всплывающая большая надпись "К оплате" гарантированно будет не меньше `minOrderPrice`.

---

## 5. Значения по умолчанию (Справочник настроек)
Все значения хранятся в `constants/index.ts` и могут редактироваться в UI калькулятора (сохраняются в `localStorage` компьютера пользователя).

| Параметр | Значение по умолчанию |
| :--- | :--- |
| Амортизация принтера | 250 ₸ / час |
| Электроэнергия | 40 ₸ / час |
| Коэффициент брака (Waste) | 1.1 (+10%) |
| Минимальный заказ | 2000 ₸ |

**Цены на материалы и наценки:**
*   **PLA:** 11 000 ₸/кг (Наценка: 200%)
*   **PETG:** 8 500 ₸/кг (Наценка: 225%)
*   **ABS:** 10 000 ₸/кг (Наценка: 180%)
*   **ASA:** 18 000 ₸/кг (Наценка: 150%)
*   **PA-CF:** 29 000 ₸/кг (Наценка: 210%)
*   **TPU:** 13 000 ₸/кг (Наценка: 170%)
*   **CUSTOM:** 10 000 ₸/кг (Наценка: 150%)
